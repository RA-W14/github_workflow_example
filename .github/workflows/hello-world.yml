name: Run main.py if it exists

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # - name: Check if main.py exists
      #   run: |
      #     if [ ! -f main.py ]; then
      #       echo "main.py does not exist!"
      #       exit 0
      #     else
      #       echo "main.py exists!"
      #     fi

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Run main.py
        run: python main.py